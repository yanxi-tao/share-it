{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build --target=bun --outfile=dist/server.js src/server.ts",
    "start": "NODE_ENV=production bun run dist/server.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.39.2",
    "hono": "^4.6.20"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.4"
  }
}
